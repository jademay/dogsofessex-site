{
  "name": "dogsofessex-site",
  "version": "1.0.0",
  "private": true,
  "description": "The local guide for dog owners in Essex — static site with a JSON data layer.",
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8000"
  }
}
